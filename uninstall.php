<?php
/**
 * Desinstalación de FIAcces.
 *
 * El plugin no guarda opciones en la base de datos (las preferencias viven en
 * el navegador de cada visitante), por lo que no hay nada que limpiar aquí.
 *
 * @package FIAcces
 */

if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
    exit;
}
